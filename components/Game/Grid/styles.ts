import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  list: {
    flexGrow: 0,
    backgroundColor: '#decfbf',
    padding: 10,
    borderRadius: 15,
    borderWidth: 2,
    borderColor: '#695c4f',
  },
});

export default styles;
